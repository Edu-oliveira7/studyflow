# StudyFlow Frontend

## Local
1. Install dependencies:
   - `npm install`
2. Run:
   - `npm run dev`

## Build
- `npm run build`

## API Base URL
- Defined in `src/lib/api.js`.
- Current local value: `http://localhost:8000`.
